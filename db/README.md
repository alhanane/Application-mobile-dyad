# Backend Build 01 — SQL

- `schema.sql` : DDL MySQL 8.
- `seeds.sql` : exemples (niveaux/classes + comptes de test).

**Comptes de test (si vous exécutez les seeds):**
- Admin: `admin / admin123`
- Parent: `parent1 / parent123`

> Remarque: le back-office crée aussi automatiquement un admin par défaut **uniquement si** la table `admins` est vide.
